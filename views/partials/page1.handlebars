<div class="homePartial">
  <ul class="homeList">
    {{#each game}}
      <li class="homeListItem">
        <img class="gameIcons" src="{{gicon}}" alt="{{gicon}}" />
        <a href="/game/{{gid}}">{{gname}}</a>
      </li>
    {{/each}}
  </ul>
</div>
<div class="pageNr"><p class="current">1</p>
  <p>//</p>
  <p>2</p>
  <p>//</p>
  <p>3</p></div>
<div class="homeForm">
  <form action="/" method="POST">
    <input type="submit" name="page1" value=">" />
  </form>
</div>